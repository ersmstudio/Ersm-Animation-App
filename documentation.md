# التوثيق الفني الشامل: محرك Ersm Animation App (V1.0)

هذا المستند يمثل المرجع التقني الشامل لبنية النظام، الخوارزميات، هياكل البيانات، ومسارات التنفيذ (Execution Pipelines) لتطبيق Ersm Animation App. هذا المستند موجه للمطورين والمهندسين لفهم آلية عمل محرك الرسوميات ثنائية الأبعاد (2D Vector Graphics Engine) الخاص بالبرنامج.

---

## 1. المعمارية الهيكلية (System Architecture)

يعتمد التطبيق على بنية برمجية تفصل البيانات (Models) عن منطق التشغيل (Tools) وعن واجهة المستخدم (Controls/Views)، مما يسهل عملية التوسع وإضافة أدوات جديدة دون التأثير على البنية الأساسية.

*   **Models:** تحتوي على تعريف البيانات فقط (لا تحتوي على منطق بصري).
*   **Tools:** واجهات (Interfaces) وأصناف (Classes) تعالج مدخلات المستخدم (الماوس/القلم) وتعدل الـ Models بناءً عليها.
*   **Controls/Views:** واجهات العرض (AvaloniaUI) التي تستمع للتغيرات في البيانات وتقوم بعملية الـ Rendering.
*   **Services:** طبقة معالجة البيانات الخارجية (حفظ/قراءة الملفات، التصدير).

---

## 2. هياكل البيانات الأساسية (Data Models)

### 2.1 كائن الرسمة (`Stroke.cs`)
يُعد الكيان الأساسي في النظام. كل خط، شكل، أو نص مرسوم يُخزن ككائن `Stroke`.

**الخصائص (Properties):**
*   `Id (Guid)`: مُعرف فريد للكائن لضمان عدم التكرار.
*   `Type (StrokeType)`: نوع الكائن (Freehand, Line, Rectangle, Ellipse, Text).
*   `Points (List<Point>)`: مجموعة النقاط الهندسية التي تُشكل المسار (Path).
*   `Color (Color)`: لون الإطار الخارجي (Stroke).
*   `FillColor (Color)`: لون التعبئة الداخلية للأشكال المغلقة.
*   `Thickness (double)`: سُمك الإطار.
*   `Opacity (double)`: نسبة الشفافية (0.0 إلى 1.0).
*   `Bounds (Rect)`: الصندوق المحيط (Bounding Box) للمستطيلات والدوائر والنصوص.
*   `Text (string)`: محتوى النص (في حال كان النوع Text).
*   `PaletteColorId (string?)`: مُعرف يربط لون هذا الكائن بلون موجود في لوحة الألوان (Palette) لتحديثه تلقائياً.

**مدخلات التحويل الهندسي (Transform Properties):**
*   `TranslateX / TranslateY`: الإزاحة في المحورين X و Y.
*   `ScaleX / ScaleY`: معاملات التكبير/التصغير.
*   `Rotation`: زاوية الدوران (بالدرجات).
*   `SkewX / SkewY`: قيم الانحراف الأفقي والرأسي.
*   `FlipX / FlipY`: قيم منطقية (Boolean) لعكس العنصر كمرآة.
*   `Pivot (Point?)`: نقطة الارتكاز المرجعية لعمليات الدوران والتكبير.

### 2.2 بنية المشروع (`AnimationProject.cs` & `Layer.cs` & `AnimationFrame.cs`)
*   **`AnimationProject`**: الكائن الجذري. يحتوي على `CanvasWidth` و `CanvasHeight` و `FPS`، بالإضافة إلى قائمة الطبقات `Layers` ولوحة الألوان `Palette`.
*   **`Layer`**: تمثل مستوى رسم مستقل، تحتوي على قائمة من الإطارات `Frames`.
*   **`AnimationFrame`**: يمثل إطاراً زمنياً واحداً، ويحتوي على قائمة `Strokes` المُراد رسمها في هذا الإطار.

---

## 3. الخوارزميات والمعادلات الرياضية (Core Algorithms)

### 3.1 خوارزمية تنعيم المنحنيات (Catmull-Rom Spline Interpolation)
أداة القلم (`PencilTool`) وأداة الفرشاة (`BrushTool`) تعتمدان على هذه الخوارزمية لتحويل النقاط المتقطعة الناتجة عن حركة الماوس إلى مسار سلس (Smooth Path).

*   **المدخلات:** قائمة بالنقاط الخام `List<Point> raw`.
*   **المخرجات:** قائمة نقاط جديدة `List<Point>` ذات كثافة أعلى ومسار منحنٍ.
*   **المنطق الرياضي:** يتم حساب نقاط وسطية (Interpolated Points) بين كل نقطتين (`p1` و `p2`) بالاستعانة بالنقطة السابقة `p0` والنقطة اللاحقة `p3`.
```csharp
double t = j / (double)segments;
double t2 = t * t;
double t3 = t2 * t;
// معادلة القطع المكافئ المتقدمة:
double x = 0.5 * ((2 * p1.X) + (-p0.X + p2.X) * t + (2 * p0.X - 5 * p1.X + 4 * p2.X - p3.X) * t2 + (-p0.X + 3 * p1.X - 3 * p2.X + p3.X) * t3);
```

### 3.2 مصفوفات التحويل الهندسي (Affine Transform Matrices)
يتم دمج إحداثيات كل كائن للرسم باستخدام مصفوفة 3x3 في الدالة `GetRenderMatrix()`.

*   **المدخلات:** خصائص `Stroke` (Translate, Scale, Rotation, Skew).
*   **المخرجات:** كائن `Matrix`.
*   **خطوات بناء المصفوفة (Order of Operations):**
    1. إزاحة نقطة الارتكاز إلى نقطة الأصل (0,0): `Matrix.CreateTranslation(-p.X, -p.Y)`
    2. تطبيق التكبير والقلب: `Matrix.CreateScale(sx, sy)`
    3. تطبيق الانحراف: `Matrix.CreateSkew(...)`
    4. تطبيق الدوران: `Matrix.CreateRotation(...)`
    5. إرجاع النقطة إلى موقعها وإضافة الإزاحة: `Matrix.CreateTranslation(p.X + TranslateX, p.Y + TranslateY)`

### 3.3 خوارزمية اختبار الاصطدام (Hit Testing)
للتأكد مما إذا كان مؤشر الفأرة يلمس كائناً قد تعرض للدوران أو التكبير، يتم استخدام تقنية "عكس المصفوفة" (Matrix Inversion).

*   **المدخلات:** نقطة الشاشة `screenPoint` ونصف قطر التسامح `hitTolerance`.
*   **المخرجات:** قيمة منطقية `bool` (تم اللمس أو لا).
*   **المنطق:**
    1. يتم جلب مصفوفة التحويل الخاصة بالكائن.
    2. يتم جلب "معكوس المصفوفة" (`matrix.Invert()`).
    3. تُضرب `screenPoint` في المصفوفة المعكوسة للحصول على `localPoint` (النقطة في الفضاء الأصلي غير المشوه للكائن).
    4. تُقاس المسافة الهندسية بين `localPoint` وأجزاء الكائن باستخدام خوارزمية "المسافة بين نقطة وقطعة مستقيمة" (`DistanceSquaredPointToLineSegment`).

---

## 4. نظام الأدوات (Tools Pipeline)

تعمل جميع الأدوات من خلال واجهة موحدة `ITool`:
*   `OnPointerPressed(canvas, e, frame)`: تُستدعى عند بدء الضغط.
*   `OnPointerMoved(canvas, e, frame)`: تُستدعى أثناء تحريك المؤشر.
*   `OnPointerReleased(canvas, e, frame)`: تُستدعى عند تحرير الضغط.
*   `Render(context, canvas)`: تُستدعى لرسم واجهات الأداة المؤقتة (Overlays).

### تفصيل عمل الأدوات:

#### 4.1 التحديد والتحويل (SelectTool)
*   **المنطق:** يقوم بعمل حلقة تكرار عكسية `for` على `currentFrame.Strokes` للبحث عن أول كائن يتقاطع مع الماوس (أولوية للطبقات العليا).
*   **حساب التكبير (Scaling):** عند سحب مقابض التكبير (Handles)، يحسب الكود فرق المسافة `(dx, dy)` محلياً، ويعدل `ScaleX` و `ScaleY`. إذا تم الضغط على زر `Alt`، يتضاعف معامل التكبير حول نقطة الارتكاز. الزر `Shift` يفرض أن يكون التكبير منتظماً (Uniform Scale).
*   **الدوران (Rotation):** يستخدم `Math.Atan2` لحساب الزاوية القطبية بين الماوس ونقطة الارتكاز وتحويلها لدرجات.

#### 4.2 الممحاة (EraserTool)
*   تتميز بثلاثة أوضاع عمل:
    *   **Full Stroke:** مسح الكائن بالكامل إذا تقاطع معه الماوس (`Strokes.Remove`).
    *   **Path Cut:** تقسيم مسار الكائن الحر (Freehand) إلى كائنين مستقلين إذا مرت الممحاة من المنتصف.
    *   **Node Cut:** تصفية العُقد (Points) الخاصة بالكائن وحذف أي نقطة تقع ضمن دائرة الممحاة.

#### 4.3 التحديد الحر (LassoTool)
*   **المنطق:** يسجل جميع النقاط كمسار مغلق (Polygon) أثناء التحريك. عند الرفع، يحسب الـ Bounding Box للصندوق المحيط للمضلع (كطريقة سريعة AABB Intersection) ويفحص تقاطعه مع الـ Bounding Boxes للكائنات الأخرى. يعين `Selected = true` لكل ما هو متقاطع.

#### 4.4 التعبئة (BucketTool)
*   **المنطق:** يبحث عن أي شكل متقاطع مغلق (مستطيل/دائرة)، إذا وجده يقوم بتعديل `FillColor` لديه. إذا لم يجد شكلًا أسفل الماوس، يقوم بإنشاء كائن مستطيل خلفية بأبعاد الكانفاس، يملأه باللون المختار، ويدرجه في نهاية الترتيب (Index 0).

#### 4.5 الكتابة (TextTool)
*   يستدعي واجهة منبثقة مستقلة `TextInputDialog`. الكود يستخدم `await` لانتظار مدخلات المستخدم. يُنشئ كائن `StrokeType.Text` ويُسند محتوى النص، ويستفيد من Avalonia `FormattedText` خلال عملية الـ Rendering لاحقاً.

---

## 5. محرك العرض (Rendering Engine)

ملف `DrawingCanvas.cs` يتحكم بعملية الرسم من خلال تجاوز (Override) لدالة `Render(DrawingContext context)`.

**خطوط مسار الـ Pipeline:**
1.  **المسح والتهيئة:** ملء الخلفية باللون المخصص `context.FillRectangle`.
2.  **Onion Skinning:** قراءة إطارات `currentFrameIndex - 1` وتمريرها للدالة `DrawFrameStrokes` بشفافية منخفضة (0.3) وألوان مميزة (أحمر للسابق، أخضر للتالي).
3.  **رسم الإطار النشط:** استدعاء `DrawFrameStrokes` للإطار الحالي بشفافية 1.0.
4.  **طبقة الأدوات (Overlay):** استدعاء `_activeTool?.Render` لرسم حدود التحديد، مؤشر الممحاة، أو مقابض التكبير/التصغير (Handles) فوق كل شيء.

**آلية رسم كائن معقد (DrawFrameStrokes):**
لكل `Stroke`:
*   يتم الاستعلام عن لونه الحقيقي من نظام اللوحة (Palette) عبر مُعرفه `PaletteColorId`.
*   يُحفظ سياق التحويل `using (context.PushTransform(GetRenderMatrix()))`.
*   يُستخدم `StreamGeometry` لفتح سياق مسار، ويتم تمرير نقاطه عبر `ctx.LineTo`.
*   تُستخدم الأقلام `Pen` المهيأة بـ `PenLineCap.Round` و `PenLineJoin.Round` لضمان أن نهايات الخطوط وزواياها ملساء، خالية من الزوايا الحادة.

---

## 6. نظام اللوحة وتزامن الألوان (Color Palette System)

بدلاً من تخصيص لون ثابت لكل كائن، تم تصميم نظام ديناميكي لمعرفات الألوان.
*   **المدخلات:** كائن لون في لوحة الألوان `PaletteColor` بخصائص `Id` و `Color`.
*   **الآلية:** أي تغيير يتم على اللون ضمن واجهة `ColorPalettePanel` يطلق حدث `PaletteColorChanged`.
*   **المخرجات:** تستقبل `DrawingCanvas` الحدث وتطلق `InvalidateVisual()`. في دورة الرسم القادمة، تستعلم الأشكال عن لونها الفعلي عبر الـ `Id`، مما يضمن تحديث كافة الإطارات الزمنية تلقائياً دون الحاجة لتغيير الكائنات واحداً تلو الآخر.

---

## 7. الخدمات ومعالجة البيانات (Services & Data Serialization)

### 7.1 الحفظ والتحميل (ProjectFileService)
*   **الآلية:** ضغط هيكل المشروع إلى صيغة أرشيف (.ersma).
*   **عملية الحفظ:** يقوم بسلسلة (Serialize) كائنات `AnimationProject` إلى `JSON` باستخدام `System.Text.Json` ويكتبها داخل الأرشيف كملفات `project.json` وغيرها، مما يضمن تقليل حجم المشروع مع الحفاظ على دقته.

### 7.2 التصدير (ExportService)
*   **الآلية:** يتم استدعاء أداة خارجية (FFmpeg) عبر سطر الأوامر باستخدام `System.Diagnostics.Process`.
*   **العملية:**
    1. يخزن المحرك كل `AnimationFrame` كملف `.png` مؤقت في مجلد `Temp`.
    2. يُمرر أمر بناء الفيديو إلى FFmpeg: `ffmpeg -framerate {fps} -i frame_%04d.png -c:v libx264 -pix_fmt yuv420p output.mp4`.
    3. يراقب حالة تنفيذ العملية، وبمجرد الانتهاء يقوم بحذف مجلد الـ Temp.

---

## 8. نظام التراجع (Undo / Redo Architecture)

يعتمد المحرك على استخدام المكدسات (Stacks) لتخزين حالات الإطار (Frame States).

*   **هياكل التخزين:** `Stack<List<Stroke>> _undoStack` و `_redoStack`.
*   **إجراء الحفظ (`SaveUndoState`):** قبل أي تعديل (رسم، تحديد، تحريك، مسح)، يتم أخذ نسخة عميقة (Deep Clone) من كافة الرسومات داخل الإطار الحالي ودفعها في `_undoStack`. تُفرّغ `_redoStack` فوراً لتفادي التناقض.
*   **إجراء التراجع (`Undo`):** يتم سحب (Pop) الحالة الأخيرة من `_undoStack`، ودفع الحالة الحالية في `_redoStack`، ثم تطبيق الحالة المسحوبة على الإطار النشط وتشغيل إعادة الرسم (InvalidateVisual).
*   **أهمية دالة النسخ العميق (`Stroke.Clone`):** تقوم الدالة بصناعة كائنات ونقاط جديدة كلياً في الذاكرة لتجنب مشكلة المراجع المشتركة (Shared References) التي قد تتسبب في تعديل حالات التراجع المخزنة مسبقاً.
